[build]
  command = "git clone https://github.com/flutter/flutter.git --depth 1 --branch stable _flutter && export PATH=\"$PATH:$PWD/_flutter/bin\" && flutter config --enable-web && flutter pub get && flutter build web --release"
  publish = "build/web"

[build.environment]
  FLUTTER_VERSION = "stable"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
